# 智能体六项能力接通与完整生成链路任务书

日期：2026-09-02  
状态：产品已确认执行，完成后待独立复核  
执行工作区：`D:\奢游\行程单报价单\奢游行程单美化-智能体试验区`  
执行分支：`agent/shadow-planner-v1`

## 一、这次要实现什么

在已经建立的智能体项目、唯一规划记录和执行调度框架上，接通真实能力，让员工在原美化工具的五步界面中完成一份真实行程成品。

用户点击“确认并生成内容”后，智能体应自行规划、核验、生成文案、搜索图片、审核图片、放入版面并完成2000px检查。只有遇到会影响事实、费用、履约或安全的关键问题时才要求用户选择；用户选择后从受影响的任务继续，不整份重跑。

本任务不是重做前端，也不是恢复旧固定工作流。应复用当前程序已经存在的解析、图片、渲染和检查能力，但由智能体计划与受控调度器决定何时调用。

## 二、给用户看的最终能力分配

| 能力 | 最终分配 | 主要职责 |
|---|---|---|
| 整程规划 `trip_planner` | DeepSeek，高推理 | 规划内容主线、模块任务、依赖、并行关系，并一次形成完整图片规划 |
| 联网事实核验 `web_fact_search` | `gemini-3.7-flash-search` | 核验已有行程中的时效事实和需要外部证据的内容，返回真实来源 |
| 文案生成 `copy_writer` | DeepSeek；普通模块中推理，复杂DAY高推理 | 按模块生成成品文案；不合格的目标模块最多由同一能力重新生成一次 |
| 品牌审查 `brand_reviewer` | DeepSeek，高推理 | 全部文案合并后独立审查一次，只输出具体问题及路径，不负责改写 |
| 图片搜索 `image_search` | 复用现有真实图片搜索链路 | 按整程规划中的图片搜索卡搜索、下载并记录候选 |
| 图片视觉审核 `visual_auditor` | 复用现有视觉模型链路 | 判断图中真实主体、图文对应、硬错误、品牌适配和三态结果 |

这六项是智能体可调度的专业能力。解析、确定性事实检查、用户确认、图片下载与放置、项目保存、取消任务、排版渲染和最终门禁仍由程序负责，不包装成大模型智能体能力。

## 三、取消两个独立能力

### 3.1 取消独立图片蓝图能力

不再把 `image_blueprint` 作为一个单独的大模型调用。

整程规划 `trip_planner` 在第一次规划时必须同时输出：

- 整体视觉主线；
- 封面图片目标；
- 每个展示酒店的主图目标；
- 每个展示DAY的主图目标；
- 有真实内容的餐饮、交通等条件模块图片目标；
- 每个图片位的地点、实体、主体、镜头方向、禁止内容、来源优先级和1—3条查询建议；
- 不同DAY之间的画面差异要求，避免连续多天都只搜索“游猎”。

程序根据真实模板和最低图片位配置校验规划。AI不能增加页面、改变必需图片位、改变行程事实或用新体验填空。

### 3.2 取消独立文案修复能力

不再把 `target_patcher` 作为独立接口，也不再进行“低推理局部修复—再次审查—继续循环”。

处理方式改为：

1. `copy_writer` 首次就读取目标模块所需的完整规则卡、已确认事实、整程主线和必要前后文；
2. 模块返回后立即进行结构、事实和字段范围的程序检查；
3. 全部文案合并后只调用一次 `brand_reviewer`；
4. 对审查命中的不合格目标模块，仍由同一个 `copy_writer` 结合具体问题和完整规则重新生成，自动重生成最多一次；
5. 只替换目标字段、目标酒店或目标DAY，其他已经合格的内容不重写；
6. 重生成后由程序做定向复检，不再调用第二轮整份品牌审查。

硬事实、费用、履约、安全或客户数据泄漏问题仍未解决时不得完成。只剩普通品牌建议且规则允许人工接受时，应在进入编辑器前让用户明确接受并保存记录，不能伪记为审查通过。

取消的是智能体中的独立能力和额外模型调用，不得删除旧固定流程仍依赖的历史底层文件。

## 四、联网事实核验的严格边界

### 4.1 允许核验的内容

`gemini-3.7-flash-search` 只核验本次行程已经存在的实体和字段，例如：

- 酒店正式名称、官网位置、公开设施与住宿体验；
- 景区、活动或交通实体的官方公开信息；
- 签证、入境、健康、疫苗和运营政策等时效信息；
- 文案确实需要、但原始资料不足以安全表达的公开事实。

不得为了“丰富内容”对整份行程进行无边界搜索。普通模块在现有资料足够时不调用联网核验。

### 4.2 每条结果必须返回

- 核验结论；
- 来源名称；
- 可访问的来源URL；
- 核验日期；
- 适用的项目字段或模块；
- 来源层级；
- 是否存在来源冲突；
- 有效期或建议再次核验时间（适用时）；
- 无法确认时的保守表述或待确认原因。

只有真实搜索并返回可追溯来源的结果才算完成。不得使用模型记忆冒充联网搜索，不得编造网址，不得把没有来源的回答写成已核验事实。

### 4.3 搜索不能改变产品安排

联网结果不得自动：

- 改变日期、人数、路线、酒店、房型、晚数、交通、费用或包含状态；
- 增加、删除或替换原行程体验；
- 把搜索发现的新项目直接写进客户文案；
- 为搜索发现的新体验建立图片任务；
- 覆盖用户已经确认的事实。

搜索发现的可选体验或优化建议只进入内部建议清单，等待定制师主动采用。采用后必须重新进入事实确认和受影响任务更新流程。

### 4.4 接口不可用时

- 先确认当前已配置的搜索服务是否真实支持模型标识 `gemini-3.7-flash-search`、联网和来源返回；
- 能力配置只为 `web_fact_search` 指定该模型，不得顺带更改文案、规划、图片或视觉审核模型；
- 不得在代码、日志、证据或前端显示API Key；
- 不修改计费配置；
- 如果当前项目缺少可用的服务配置或该模型无法返回真实来源，停止这一能力的接通并报告一个明确阻塞项，不得自行发明接口地址、密钥或用普通模型回答降级冒充。

## 五、正式运行顺序

1. 程序解析上传资料并完成确定性检查；
2. 生成前集中解决可提前发现的事实、费用、履约和安全问题；
3. `trip_planner` 高推理生成唯一整程计划，计划同时包含完整图片规划；
4. 调度器按计划开放任务：互不依赖的联网核验、文案模块和图片候选搜索可以并行；
5. 依赖某条联网事实的文案只等待该事实，不等待所有搜索结束；
6. 文案模块完成后立即做程序级结构与事实检查；
7. 所有文案合并后调用一次 `brand_reviewer`；
8. 需要改写的目标模块由同一 `copy_writer` 最多重新生成一次并定向复检；
9. 图片按规划搜索、下载即进入视觉审核队列，不得等全部下载后才开始审核；
10. 程序完成去重、图文一致检查和自动放置；
11. 程序渲染2000px成品并对真实版面做最终检查；
12. 全部生成和检查完成后才显示100%并进入原编辑器。

不得恢复“整份文案一次生成—整份复核—整份重写”的串行流程，也不得等待全部图片搜索完成后再统一开始视觉审核。

## 六、图片完成规则

- 整程规划必须让不同DAY的图片体现当天不同的地点、动作、场景、节奏或记忆点，不能因为连续游猎就把每天都规划成同一种游猎画面；
- 最低覆盖为封面1张、每个展示酒店1张主图、每个展示DAY 1张主图；
- 有真实内容且最终显示的其他图片位也必须有真实可用图片；
- 没有真实内容的条件模块自动隐藏；可选图片位找不到合格图片时应删除该图片位并重排，不能留下空框；
- 所有最终显示的图片位不得为空，不得使用占位图、破图、硬拒绝图或AI生成图；
- 审核服务超时、限流、网络错误或非法返回只进入 `manual_review`，不能当成图片硬错误；
- 每个图片位最多两轮搜索，图片技术审核最多一次技术重试；
- 用户锁定和上传的真实图片不得被自动任务覆盖；
- 授权状态与生成完成状态分离：生成可完成并进入编辑器，但正式导出仍受商业授权门禁控制。

## 七、调度和速度要求

- 目标是标准7—10日真实行程完整流程在20分钟内完成，记录总耗时和每个能力的实际耗时；
- 文案按相互独立的模块组并行，初始并发2，真实稳定后最多3；
- 图片搜索和视觉审核使用连续队列，下载一张即可审核一张；
- 联网事实只核验需要核验的字段，并使用字段级缓存；
- 不能为了提速无限增加外部调用、重试和费用；
- 每次运行记录各能力请求数、输入/输出Token、耗时、失败、重试和可得费用；
- 建议总消耗超过原固定流程的130%时标记“待优化”，但不得因此隐藏质量问题；
- 页面持续显示真实阶段、任务数量、耗时和等待原因，不展示模型推理正文；
- 生成页默认使用轻量的“实时进度总览”：资料检查、智能规划、事实核验、文案生成、图片准备、审核排版、成品检查七个用户阶段；原右侧无操作价值的项目预览占位卡不再保留；
- 26个内部任务、计划ID、优先级、调用统计和技术错误默认收进“查看本次规划/技术详情”，不得铺满普通员工页面；
- 进度只能由真实任务事件和完成门禁更新，禁止假倒计时、假动画和未接通能力的模拟进度；
- 用户可取消整个任务，取消不删除项目，进行中的外部请求应尽快中止，已完成结果保留为运行证据但不得当成正式成品。

20分钟是第一版的实用目标，不得通过跳过必需事实检查、品牌检查、图片审核或2000px检查伪造达标。

## 八、完成状态和异常处理

### 8.1 可以到100%的条件

- 生成前关键问题已经解决；
- 文案、图片和版面所需任务真实完成；
- 硬事实、费用、履约、安全和客户数据隔离检查通过；
- 所有最终显示的图片位都有真实可用图片；
- 2000px实际版面检查通过；
- 没有仍在运行、等待重试或等待用户确认的任务。

进入编辑器即表示智能体本次生成任务完成。用户在编辑器中的普通人工文字修改不自动调用智能体，也不被系统改回；只有用户主动选择需要AI的功能时才创建新的目标任务。

### 8.2 运行中需要确认

只有新发现的事实、费用、履约或安全关键选择才弹出确认。页面显示来源、影响、选项和推荐理由；用户确认后只恢复受影响任务，无关任务继续保留。

普通技术失败先按该能力预算重试；仍失败时显示具体能力和原因。不得让进度停在100%，不得把失败输出当成功，也不得静默回退到旧固定流程。

## 九、技术实现要求

### 9.1 复用底层能力，不复用旧编排

优先用适配层复用现有：

- Excel解析、事实来源和字段门禁；
- DeepSeek客户端与模块文案生成；
- 现有图片搜索、下载、账本、去重和视觉审核；
- 客户数据白名单、渲染、最终输出QA和版面检查；
- 项目状态、任务取消和证据记录。

不得直接调用旧版“从头跑到尾”的总流程。每个底层能力必须由当前 `planId`、`executionRunId` 和目标任务调用，并只写入允许字段。

### 9.2 能力适配器统一要求

每个真实能力至少定义：

- 输入和输出结构；
- 可读事实范围；
- 可写字段白名单与禁止字段；
- 本次规则卡和规则版本；
- 模型或服务配置引用；
- 超时、重试、并发和费用预算；
- 状态与进度事件；
- 取消信号；
- 请求、结果、来源和错误证据；
- 幂等键，避免页面刷新或恢复任务造成重复收费。

任何能力试图修改计划外字段、事实或用户锁定内容时，程序拒绝结果。

### 9.3 配置与文档同步

- 把智能体正式能力目录更新为本任务书确定的六项专业能力；
- 将 `target_patcher`、`image_blueprint` 从智能体能力目录和新任务路由中取消；
- 将目标文案重生成路由到 `copy_writer`；
- 将图片计划字段合并进 `trip_planner` 的结构化输出；
- 把 `web_fact_search` 的模型配置为 `gemini-3.7-flash-search`；
- 同步更新《智能体规则与能力调度规范》和规则落实对照证据，避免文档、配置、计划和运行时出现双重标准；
- 01—06仍是内容、事实、图片和版式的唯一正式规则，不得另建一套相互冲突的智能体内容规则。

### 9.4 执行开关

- 规划记录继续保持不可变的事实计划，不在计划对象上反复改写执行结果；
- 只在4174的 `agent_v1` 测试配置中开放真实执行；
- 执行运行记录可以明确为已授权执行，并引用唯一 `planId`；
- 4173和旧固定流程的开关、完成状态、项目数据及模型配置完全不改；
- 禁止前端直接绕过调度器调用模型或图片服务。

## 十、实施顺序

最终执行顺序改为“真实能力在前，实时前端在后”：

1. 先检查当前前端纠偏修改。如已有未提交成果，只做独立安全检查点提交，不回滚、不清理，也不继续用假数据完善页面；
2. 定义六项能力的真实输入输出、统一任务状态和统一进度事件；
3. 接通六项真实能力和程序完成门禁，用后端测试证明状态、进度、确认、重试和取消真实可用；
4. 再完成《智能体接入原美化工具前端纠偏任务书》，把真实事件接入轻量实时进度总览；
5. 最后使用一份覆盖面足够高的真实项目进行前后端完整回归。

原因是前端总进度、七个阶段、等待原因和100%完成状态必须来自真实执行链路。先制作最终前端会依赖假状态，接通能力后还要返工。

能力实现与前端最终接入不得混成一个不可追踪的大提交；按下列检查点分别提交。

本任务可以在同一执行窗口内按四个内部检查点连续完成：

1. 联网事实核验、整程规划、模块文案和统一进度事件；
2. 图片规划合并、搜索、视觉审核和放置；
3. 渲染、完成门禁、确认、取消与恢复；
4. 基于真实事件完成轻量前端，并进行完整真实回归。

每个检查点单独提交，遇到真实接口、密钥或供应商能力阻塞才向用户报告；不因低影响实现细节反复询问用户。

## 十一、不允许改变的内容

- 不修改 `D:\奢游\行程单报价单\奢游行程单美化-本地版`；
- 不修改 `D:\奢游\行程单报价单\奢游行程模板系统`；
- 不修改4173旧固定流程；
- 不回滚、覆盖、清理或隐藏任何已有提交和未提交修改；
- 不新建第二套员工前端、编辑器、模板或成品渲染器；
- 不让AI改变确定性事实、费用、履约状态、用户确认或图片位必要性；
- 不让联网搜索自动扩展产品安排；
- 不恢复独立 `target_patcher` 或独立 `image_blueprint` 调用；
- 不用AI生成图片兜底；
- 不输出或提交API Key，不修改计费配置；
- 不宣布智能体已经替换旧流程，真实回归和独立复核前只标记“开发完成，待独立复核”。

## 十二、验收标准

### 12.1 能力和路由

- 一次运行的真实能力清单与六项分配一致；
- `trip_planner` 输出完整内容规划和图片规划，不再发生独立图片蓝图模型调用；
- 审查后目标模块由同一 `copy_writer` 最多重新生成一次，不再调用独立文案修复模型；
- `brand_reviewer` 每份成品只运行一次；
- 每次调用都有规则版本、目标路径、耗时、重试、Token/费用和证据；
- 未授权能力和越权字段修改被程序拒绝。

### 12.2 联网事实核验

- 运行日志证明 `web_fact_search` 使用 `gemini-3.7-flash-search`；
- 至少验证酒店官方事实、普通公开实体事实和一项时效政策三类场景；
- 每条采用事实具有真实可访问来源URL、来源名称和核验日期；
- 无来源、来源冲突和接口失败分别进入正确的保守表达或确认状态；
- 搜索发现的新体验只进入内部建议清单，客户文案、图片位、费用和每日安排均未被自动改变。

### 12.3 文案和图片

- 每个文案模块实际收到适用的完整01—06规则卡，不只收到几句规则摘要；
- 重复DAY能够生成当天不同的场景、节奏和价值，不连续输出相同套话；
- 目标重生成不修改其他合格模块或确定性事实；
- 每个最终显示图片位都有真实可用图片；
- 封面、展示酒店和展示DAY满足最低主图覆盖；
- 无破图、错地点/主体、重复、严重裁切、空白图片框、硬拒绝图和AI生成图；
- 图片来源、候选、状态、失败和用户决定可追溯。

### 12.4 前端和完成门禁

- 用户仍在4174的原五步美化工具前端操作；
- 点击生成后在原生成页面看到真实规划、核验、文案、图片、审核、渲染和检查进度；
- 默认页面简洁展示总体百分比、当前动作、七个阶段、已完成任务数和已用时间；详细计划与技术数据默认折叠；
- 原右侧“当前项目/成品预览尚未开放”占位卡已经取消并替换为实时进度总览，左右区域不重复展示同一批信息；
- 运行中关键确认在当前页面处理，保存后从目标任务继续；
- 未通过时不显示100%，不进入编辑器；
- 全部完成后才显示100%并进入原编辑器；
- 用户取消后任务进入已取消状态，项目和证据保留；
- 不展示模型推理正文、内部字段、图片评分、失败堆栈或密钥。

### 12.5 真实回归

使用1份全新、覆盖面足够高的真实项目，尽量同时包含：

1. 连续多天相似路线，需要明显区分每日文案和图片；
2. 多酒店、多交通和较多条件模块；
3. 缺失、冲突、可选体验或时效事实，需要确认和联网核验。

该项目从上传运行到可进入编辑器，并保存：输入摘要、计划、调用时间线、事实来源、文案结果、品牌问题及目标重生成、图片规划字段、图片账本、最终2000px长图、完成门禁结果和总耗时。项目隔离、取消、授权和自动化门禁另行专项验证，不以增加真实项目数量替代。

标准7—10日项目以20分钟内完成为目标；如未达到，必须按能力列出真实瓶颈，不能删除检查或伪造时间。

另外必须证明：

- 4173旧固定流程仍可运行且结果未改变；
- 4173和4174的项目、缓存、任务和完成状态相互隔离；
- 相关单元、契约、越权、取消恢复、构建和站点测试通过；
- 不存在密钥或客户内部数据泄漏。

## 十三、证据与交接

建立证据目录：

`audit/evidence/2026-09-02-智能体六项能力接通与完整生成链路/`

至少提交：

1. 给用户看的完成说明；
2. 最终六项能力和模型/服务分配；
3. 更新后的调度规范、配置和规则落实对照；
4. 每项能力的输入输出契约与越权测试；
5. `gemini-3.7-flash-search` 真实搜索与来源证据；
6. 一份综合真实项目的全链路证据；
7. 4173/4174隔离与旧流程回归证据；
8. 每阶段耗时、调用数、Token/费用和失败重试统计；
9. 修改文件、提交、推送状态和剩余风险；
10. 待独立复核清单。

## 十四、给执行窗口的简短指令

执行顺序已经纠正为先能力、后前端。先把当前已有前端修改作为独立安全检查点保存，不回滚、不清理，也不要继续用假数据制作实时页面。随后接通六项真实能力和统一进度事件：DeepSeek高推理整程规划（同时完成图片规划）、`gemini-3.7-flash-search` 联网事实核验、DeepSeek模块文案生成、DeepSeek高推理品牌审查、现有图片搜索、现有视觉审核。取消独立 `image_blueprint` 和独立 `target_patcher`；品牌审查后只允许同一 `copy_writer` 对不合格目标模块重新生成一次。联网搜索只能核验和丰富已有安排，必须有真实来源，发现的新体验只进内部建议，绝不能自动改行程。真实任务状态、确认、重试、取消和完成门禁跑通后，再完成轻量实时进度前端。复用旧程序底层能力但不要复用旧固定编排；4174开放测试执行，4173完全不改。最后使用一份覆盖面足够高的真实项目完成全链路回归，全部内容、图片和2000px检查通过后才到100%并进入原编辑器，目标时间20分钟内。完成后只标记“开发完成，待独立复核”。
