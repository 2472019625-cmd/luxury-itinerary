# STEP4-MANUAL-20260908 执行交接

状态：本地实现及隔离回归通过；未部署此补丁，线上真实项目 A/B/C/D 待验证，待独立复核。

## 已定位与修改

- 已完成 Simple 项目存在进入通用 Editor、缺少 select/upload 回调的路径。完成跳转和重新进入已完成项目统一到现有 `/simple/projects/:id`，不改变生成执行器。
- 原 select/upload/research 保存函数等待整张 Renderer 完成才保存并返回。现先写 final-result 中当前 slot，再返回项目数据；既有 Renderer 在人工编辑适配层串行后台检查。旧 output 立即失效，仍只有原检查通过才开放正式输出。没有修改 Renderer、Planner、Copy、Pipeline 或 Image Skill 文件。
- 文件验证/上传/搜索结束后重新读取最新项目，避免慢请求覆盖新选择。后台渲染通过 revision 防止旧结果覆盖新绑定。
- 写回仅处理目标与移动来源，保留其他图片及已保存文案；前端合并图片字段、拒绝迟到旧版本，保留等待期间其他文案编辑。
- Modal 使用内嵌确认与状态反馈；状态由 Editor 持有，ESC/关闭不取消搜索。重新打开拉取最新候选。搜索前端等待有240秒上限，失败不会无限旋转；已发出的服务端请求仍可结束并保存，再打开拉取结果。
- 上传绑定、保存、刷新与候选选择使用相同链路。图片成功/失败、上传成功/失败、搜索开始/新增数/无新增/失败均有非原生提示。

## 文件

业务修改：`app/server/simple-manual-images.mjs`、`app/server/agent-planner-app.mjs`（仅人工接口延后渲染参数）、`app/src/AgentWorkspace.jsx`、`app/src/Workspace.jsx`、`app/src/workspace.css`、`app/src/lib/manualImageState.js`。

测试：`app/tests/simple-manual-images.test.mjs`、`app/tests/support/manual-image-fixture.mjs`、`app/scripts/verify-step4-manual-images.mjs`。

交付：`app/scripts/package-step4-update.mjs`、`app/scripts/deploy-step4-update.py`。

工作树原有字体、鉴权及其他未提交内容保留；未写入冻结本地版，未提交或回滚用户工作。

## 证据

- `npm run build` 成功；保留已有大 chunk / dynamic import 提示。
- 29 项 Node 测试通过：manual-images、simple-pipeline-server、simple-pipeline-isolation、sites-worker、demo-auth、public-static、agent-planner-frontend。
- Chrome生产构建 + 新建隔离项目 + 真正的 manual-images 后端函数和磁盘存储通过：候选即时/关闭再开/刷新；上传即时/关闭再开/刷新；搜索启动/新增/空结果/失败；搜索中ESC与关闭及重新打开；操作其他模块；替换与上传保存失败保留旧图。无 pageerror、无原生弹窗。
- 证据：`audit/evidence/2026-09-08-step4-manual-images/browser-result.json`、`save-error-feedback.png`。
- 测试使用新夹具数据，搜索与 Renderer 为慢响应 mock；不是线上真实业务项目，也不是2000px真实成品验收。

## 交付包

`D:/奢游/行程单报价单/demo-access/sheyou-step4-update-TEqCbO.tar.gz`，333651 bytes（约326KiB）。

SHA256：`83739c886b21628c032e39a7b19a228994e6f1e2c44a463b3295df4c0d62ab14`。

仅5个线上文件：两个server模块、入口HTML、主JS、主CSS。不重新包含字体，不修改Nginx/鉴权配置/项目数据。安装器要求先前font-update版本匹配；不匹配立即拒绝，不强制覆盖。重启前备份，健康检查失败恢复旧文件。

## 剩余与边界

- 需要用户上传安装，且确认没有生成或导出任务；重启后重新登录。然后用线上可修改项目做A/B/C/D和最终下载校验，不能宣布线上修复完成。
- 后台渲染是当前进程内队列，不是持久任务系统。服务重启会中断未完成检查，已保存图片仍存在；最终输出保持关闭，需要重新触发人工图片操作检查。没有为本任务引入任务平台。
- 多浏览器同时修改的协作一致性未验收；当前修复覆盖单编辑页的搜索与手动保存竞争。
- 既有人工确认覆盖硬拒绝候选行为与 IMG-014/FLOW-005 存在旧规则冲突，本轮未扩大修改候选资格或搜索规则；保留已有测试行为，需产品独立处理，不作为本轮认可。
- 普通文案/焦点等其他手动编辑的全量云端保存不在本轮范围，不承诺其持久化。
