# Planner 精简前置门禁报告

状态：暂停在实施前置核查；不是实现完成或验收通过。

## 已完成

- 正式规则03更新至1.17：DAY主题/正文保留原有主要视觉；图片卡及对应体验文案使用各自绑定图片位画面，不共享当天主图；无法等价合并的旧字段保留。
- 正式规则04更新至2.3：补充新版visualPlans契约、visual原样映射、自然同框判断、合格Query不变和既有Scope策略冻结。下游业务规则未变。
- 任务书更新至1.2：纠正图片卡共享DAY主图的歧义，取消缩短20%的硬验收，保留旧DAY视觉字段。
- 只读核查当前Planner、Adapter、Query准备脚本及Copy输入路径。未改生产代码、模型配置、知识库、审核、Step4、Copy业务规则或Renderer。

## 暂停原因：工作分支冲突

工作目录正确：`D:\奢游\行程单报价单\奢游行程单美化-智能体试验区`。

- AGENTS.md指定专用分支：`agent/shadow-planner-v1`。
- 实际分支：`codex/simple-pipeline-frontend-v1`。
- 当前HEAD：`2f3c734fc374b8255ab1b06f80b41aa9ea4755f4`。
- 规则指定分支存在，当前指向`f470779`，不是当前工作基线。
- 当前Planner和Adapter相对HEAD已有463行新增、68行删除；还有其他未提交文件。这些均作为既有成果保留，没有切分支、stash、reset、restore、提交或推送。

待用户确认：本轮是否以试验区当前分支及全部既有改动为基线继续。建议沿用当前基线并明确本轮分支例外，避免把旧分支当作当前实现。

## 修改前定向测试

工作目录：试验区`app/`。

命令：`node --test tests/day-visual-alignment.test.mjs tests/day-search-title-split.test.mjs tests/simple-planner-contract.test.mjs tests/planner-technical-failure.test.mjs`

结果：29项，28通过，1失败。

失败位置：`app/tests/day-search-title-split.test.mjs:10`。旧预期要求搜索表达额外追加`photos`，当前函数实际只返回原搜索表达。该项不是本轮修改引入；未为通过测试重写搜索代码，也未修改旧测试。后续应按已确认的Query直通规则更新这一测试，不恢复追加泛词行为。

## 已确认的兼容核查点

- DAY主题与正文通过`relevantContext.dayRole`接收旧主要视觉；本轮保留，不直接删除。
- 独立视觉卡通过`facts.visualSubject/titleCoreSubject`接收当前图片位画面；必须保留逐位绑定。
- 原始普通Spot文案与独立视觉卡不是同一个任务，不能用当天主图统一覆盖。
- 当前DAY画面还经过`cleanDayVisualSubject`；新格式原样映射必须与旧格式隔离并做对照，不能机械删除旧项目清洗分支。
- 当前来源匹配与图片存放位置分别计算；实施时必须分别核对sourceRefs、费用状态、slotBindings、copyTargetId和图片卡标题，不得只比较图片主体文字。

## 尚未执行

未创建候选生成器，未改Planner或Adapter，未运行精简后的真实Excel dry-run，没有精简前后输入一致性的结论。

本轮真实Planner调用、知识库请求、下载、视觉审核、Copy生成、Renderer生成均为0；未部署、未覆盖旧项目。定向自动测试使用测试数据，不等同于真实Excel验证。

分支边界确认后，继续保存基线、候选与契约单测、最小实现、下游输入对照、无知识库真实dry-run；完成后停下供用户确认。
