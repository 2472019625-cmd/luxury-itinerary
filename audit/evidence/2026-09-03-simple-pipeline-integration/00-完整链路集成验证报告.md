# simple-skill-pipeline 完整链路集成验证

日期：2026-09-03
分支：`agent/simple-skill-pipeline-v1`

## 验证范围

本次使用全新合成 Excel 报价单夹具，依次经过真实 `importItineraryWorkbook()`、现有 `generateAgentPlan()`、simple Planner 契约适配、真实 `runCopyWriterSkill()` 与 `runImageSearchSkill()` 入口、确定性写回、项目保存、真实 `renderer/render.mjs --width=2000`。

本次是完整链路集成测试，不是正式客户项目回归。Planner、Copy、搜索和视觉供应商调用使用确定性集成适配器；现有模板与浏览器 Renderer 为真实运行。

## 最终成功路径

- `projectId`: `dbb29872-f62f-49cc-a747-b17dd409bed5`
- `pipelineStatus`: `complete`
- Parser：9ms，1次
- Planner：7ms，1次模型边界调用
- Copy Skill：72ms，1个业务批次，1次批量模型边界调用
- Image Skill：286ms，1个业务批次，7个 slot，7次搜索，7次网页提取，7次下载，7次批量视觉判断
- Program 确定性写回：1ms
- Renderer：3964ms，1次
- 保存：23ms
- 总墙钟：4292ms
- Copy/Image 启动时间差：1ms
- Copy/Image 实际重叠：71ms
- Image 并发峰值：slot 3、搜索 3、网页 1、下载 1、视觉 1

## Renderer 结果

- PNG 宽度：2000px
- PNG 高度：26876px
- 文字/模块溢出：0
- 破图：0
- 异常大空白：0
- 固定品牌页脚：存在

## 失败隔离与完成状态

自动化测试同时覆盖：

- DAY2 Copy 单项失败时，其他 Copy 和 Image 单元继续并保存；
- 展示酒店必需图片 `not_found` 时，其他图片继续；
- 可选交通图片 `not_found` 时移除该图片位；
- 必需图片缺失时 Renderer 不启动，项目不进入 `complete/100%`；
- 全部必需项满足且真实 Renderer 成功后才进入 `complete/100%`。

## 旧复杂路径隔离

运行事件共记录30条能力事件，未观察到 brand Reviewer、`review_decision`、finding package、Copy 自动重生成、图片第二轮搜索、自动定向重搜或 stage budget。自动 Copy 重生成轮数为0，Image 自动后续搜索轮数为0。另有静态隔离测试确保 Simple Pipeline 调度图不引用旧审核、目标修复和业务重搜入口。

机器可读明细见同目录 `full-chain-smoke-summary.json`。
