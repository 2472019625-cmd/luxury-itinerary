# 剩余冲突与待独立复核清单

状态：产品决定已写回，待独立复核

## 产品决定状态

DEC-001—DEC-006 均已确认，没有遗留“待产品决定”项。

## 待后续程序实现

- 建立最低图片位的版本化产品配置，并拒绝AI改写必需/可选属性。
- 实现可选图片位移除重排、必需位等待确认和2000px逐slot真实可用检查。
- 实现 `user_accepted_suggestion`、证据字段和硬问题禁用门禁。
- 实现商业授权独立状态及正式导出门禁，不能用来源账本推断授权。
- 实现4174默认端口、受控回退记录和旧流程4173隔离。
- 实现补充资料白名单校验；影子阶段仍不得真实解析或调用OCR。
- 第一批不实现删除/回收站；后续如开发30天回收站必须另立任务。

## 已知基线差异

`npm run test:rule-coverage` 的已知差异为 `FLOW-004`：正式规则执行类型为 `A+C+D`，旧运行配置登记为 `A+C`。这是下一批程序整改输入，本批不得修改应用配置来掩盖。

## 独立复核重点

- 所有当前智能体口径是否都使用100%而非98%。
- 三处最低视觉覆盖是否完全一致，AI是否仅能建议可选增量。
- 授权待确认是否不影响100%/编辑器但确实阻止正式导出。
- `user_accepted_suggestion` 是否从未被称为passed，且硬问题清单完整。
- 补充资料白名单、无OCR、不得覆盖Excel/用户确认事实是否一致。
- 第一批是否没有删除、回收站能力或界面承诺。
- 4174与受控回退是否明确，4173只属于固定流程。
