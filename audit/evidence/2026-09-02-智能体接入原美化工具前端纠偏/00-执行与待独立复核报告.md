# 智能体接入原美化工具前端：执行与待独立复核报告

## 本次结论

已将 `4174/agent` 的员工正式入口接回原美化工具的五步工作台：上传资料、确认信息、生成内容、编辑预览、下载版本。原先单独开发的 `AgentWorkspace` 已降级为内部诊断页，不再作为员工入口。

本批次只开放真实已经接通的资料解析、关键确认、计划制定与执行准备能力。编辑预览、下载版本继续锁定；没有伪造进度，没有调用文案生成、事实搜索、图片搜索、自动放置、渲染或导出。

## 用户现在会看到什么

- 4174 与原 4173 使用相同的五步页面结构和操作习惯。
- 上传后先进入原确认页；费用、履约等关键问题直接嵌入确认页，不再跳到另一套系统。
- 确认后停留在原“生成内容”页，只显示当前真实阶段和动作，不显示虚假百分比。
- 当前有效计划在生成页内折叠展示，展开后读取同一个 `activePlanId`，没有另存一份页面计划。
- 后续若执行中出现事实、费用、履约或安全关键问题，生成页已具备内嵌确认卡和“保存选择并从当前任务继续”的交互承载；当前批次尚未开放会触发这些后续问题的真实下游能力。
- 编辑预览与下载版本明确显示“尚未开放”，不会出现伪造入口。
- 4174 浏览器数据使用 `sheyou-agent-*` 独立命名空间，首批智能体项目不显示删除入口。

## 修改范围

- `app/src/App.jsx`：`/agent` 改用原 `Workspace`；内部诊断页迁移到 `/agent-diagnostics`。
- `app/src/Workspace.jsx`：增加智能体模式适配、独立存储、项目接入、关键确认、轮询、取消、生成页计划展示与后续门禁锁定。
- `app/src/workspace.css`：补齐与原工作台一致的智能体状态、确认、计划与锁定态样式。
- `app/src/AgentWorkspace.jsx`：明确标记为内部诊断工具，并调整诊断路由。
- `app/tests/agent-planner-frontend.test.mjs`：覆盖原五步入口、独立存储、同一有效计划、零下游调用和生成页确认续跑契约。
- `app/scripts/agent-frontend-correction-e2e.mjs`：全新浏览器数据下对比 4173/4174，并验证正常项目与关键确认项目。
- `app/scripts/agent-frontend-existing-visual.mjs`：复用已有计划做无模型调用的视觉复拍。
- `audit/tasks/2026-09-02-智能体接入原美化工具前端纠偏任务书.md`：本次执行任务与确认后的交互口径。

未修改模型、API Key、接口地址、计费配置或任何 `.env` 文件，也未写入固定流程目录。

## 全新项目真实回归

### 正常项目

- 源文件：`【行程内容】坦桑尼亚10日顶奢（11.4W起）：Singita+罗莱夏朵+Melia+Nimali.xlsx`
- 智能体项目：`78a1b883-afa9-4c24-9f6f-20cf9b3b1fba`
- 状态：`ready_for_execution`
- 当前有效计划：`8bba75b9-55b7-441f-891e-e8e383f1f920`
- 计划任务：26
- 执行检查记录：`19cf2cec-cd76-46af-aa27-f09c77fc3ace`
- 页面计划、项目 `activePlanId` 与执行检查引用计划一致。
- 下游能力实际调用：0。
- 页面无百分比进度，无编辑或下载开放动作。

### 关键确认项目

- 源文件：`【行程内容】 肯尼亚8日顶奢（8.4W起）：Angama+丽思卡尔顿+Saruni+JW万豪.xlsx`
- 智能体项目：`8938f561-607c-4906-95eb-9ff2162f8796`
- 状态：`awaiting_confirmation`
- 待确认问题：1
- `activePlanId`：空
- 结论：关键确认未完成前没有调用规划器，也没有生成计划。

## 页面证据

证据目录：`audit/evidence/2026-09-02-智能体接入原美化工具前端纠偏/页面截图/`

1. `01-4173-固定流程上传页.png`：4173 原固定流程五步上传页。
2. `02-4174-智能体上传页.png`：4174 同一五步上传页，仅增加智能体试验和独立数据提示。
3. `03-4174-正常项目确认页.png`：正常项目沿用原确认页。
4. `04-4174-智能体生成页.png`：真实状态、任务数量、零实际完成、零下游调用、折叠计划和后续门禁。
5. `05-4174-生成页展开规划.png`：在原生成页展开同一有效计划。
6. `06-4174-关键确认页.png`：关键问题内嵌在原确认页。
7. `frontend-correction-validation.json`：全新项目浏览器回归原始结果。

## 自动化结果

- `npm run test:rules`：71/71 通过。
- `npm run test:images`：28/28 通过。
- `npm run test:import`：3/3 通过。
- `npm run test:sites`：4/4 通过。
- `npm run test:agent-planner`：24/24 通过。
- `npm run build`：通过；仅保留既有的 Vite 大包与动态/静态导入提示，没有新增构建失败。
- 4173 首页：HTTP 200。
- 4174 智能体入口：HTTP 200。

## 剩余边界与待独立复核

- 当前计划仍是 `plan_only`，真实文案、联网事实搜索、图片、审核、自动放置、2000px 检查、编辑和正式导出都未开放，这是本批次有意保留的安全边界。
- 生成页已实现运行中关键确认的界面与续跑请求契约，但因下游调度能力尚未开放，本批次不能用真实后续任务触发该场景；独立复核时不得把“界面已承载”误判成“后续执行能力已完成”。
- 请独立复核 4173 未受影响、4174 的五步交互一致性、关键确认门禁、同一 `activePlanId`、独立存储、项目不可删除以及所有下游调用为 0。
- 执行窗口不宣布独立验收通过，也不关闭台账。
