# 冲突与产品决定清单

日期：2026-09-02
状态：产品决定已写回，待独立复核

## 一、原适用范围冲突处理结果

| 编号 | 最终处理 |
|---|---|
| SCOPE-001 | 01、03已区分固定流程冻结98%行为与智能体100%门禁；98%不是智能体运行值。 |
| SCOPE-002 | 00已删除智能体“预算结束留空”口径，并写明最低视觉覆盖和可选位移除重排。 |
| SCOPE-003 | 00、06和蓝图已写明智能体唯一开发源是试验区；固定流程目录只保护冻结基线。 |
| SCOPE-004 | 智能体默认4174，冲突时受控选择其他本地端口并记录；不得占用或误连4173。 |
| SCOPE-005 | 普通品牌表达建议使用 `user_accepted_suggestion`，允许继续但不标 `passed`；硬问题不可绕过。 |

## 二、DEC-001—DEC-006 已确认

| 编号 | 最终产品决定 | 状态 |
|---|---|---|
| DEC-001 | 新智能体默认 `127.0.0.1:4174`；4174占用时选择受控其他本地端口并记录实际值，保持入口、浏览器数据和测试项目隔离，绝不占用或误连固定流程4173。 | 产品决定已写回，待独立复核 |
| DEC-002 | 普通品牌表达建议经用户接受后记为 `user_accepted_suggestion`，保留问题/规则ID、用户决定和时间。它是允许终态但不是 `passed`；事实、费用、履约、安全、内部信息、结构和必需图片硬问题禁用。 | 产品决定已写回，待独立复核 |
| DEC-003 | 最低视觉覆盖为封面至少1张、每个实际展示酒店模块至少1张主图、每个实际展示DAY至少1张主图。第二张、装饰图和细节图为可选位，无合格图时移除重排。必要性由产品配置和程序判定，AI只可建议可选增量。 | 产品决定已写回，待独立复核 |
| DEC-004 | 主行程只接受一份受支持Excel；补充白名单是可提取文本PDF、DOCX、TXT、Markdown及用户授权图片。只作补充文本证据，不覆盖Excel或用户确认事实；不做OCR、不接旧`.doc`/聊天记录/任意文件主输入；提取失败要求粘贴文字或可读文件。影子阶段只记录类型、来源、用途。 | 产品决定已写回，待独立复核 |
| DEC-005 | 内容、图片、版面和2000px门禁通过后可100%进入编辑器；商业授权独立于生成完成。授权待确认时可编辑且状态保持完成，但正式导出被锁。用户确认权利或管理员预批准素材后放行；拒绝后替换、补授权或停止导出，不触发重生、重审或98%。 | 产品决定已写回，待独立复核 |
| DEC-006 | 回收站不进入第一批。第一批项目全部保留，不提供永久删除、回收站能力或界面承诺；30天回收站另立后续项目生命周期任务。 | 产品决定已写回，待独立复核 |

## 三、仍需程序实现和独立复核的内容

- 六项决定均已消除产品待决策项，但不代表智能体程序已实现。
- 下一批需要实现版本化必要性配置、允许终态、授权导出门禁、4174启动隔离、补充资料白名单校验及首批无删除入口。
- `test:rule-coverage` 已知基线差异仍是 `FLOW-004`：正式规则执行类型 `A+C+D`，旧运行配置登记为 `A+C`。本批不改应用配置，必须在后续程序任务中修正并补人工责任证据。
- `candidateStatus`、`slotCompletionStatus` 与授权导出状态必须分开，不能相互推导。

当前没有未裁定的 DEC 项；整体状态只能写“产品决定已写回，待独立复核”。
