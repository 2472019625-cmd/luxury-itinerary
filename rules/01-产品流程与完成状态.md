# 01 产品流程与完成状态

版本：1.6
生效日期：2026-09-02
适用范围：从资料上传到编辑、导出、执行交接与独立审查的产品流程和完成状态。数据、文案、图片、视觉和运行细则分别服从 02—06。

## 执行类型

- `A 程序强制`：确定性流程或状态门禁。
- `B AI生成`：模型承担的文案或视觉策划阶段。
- `C 生成后检查`：生成、放置或渲染后的质量检查。
- `D 人工验收`：业务、授权、审美或独立复核。

## 最终规则

| 规则ID | 最终规则 | 执行类型 | 输入或依据 | 失败/异常处理 | 不适用边界 | 验收要求 | 当前落实状态与证据入口 |
|---|---|---|---|---|---|---|---|
| FLOW-001 | 标准主流程固定为：单份 Excel 上传 → 确定性解析 → 人工事实确认 → 生成整程内容与视觉主线 → 客户文案模块与图片蓝图/候选搜索并行 → 独立品牌审查及目标字段/DAY局部修复 → 最终文案—图片一致性检查与自动放置 → 实际 2000px 版面检查 → 编辑器 → 2000px PNG 导出。并行只改变等待顺序，不省略任何阶段。 | A+B+C | 用户确认事实、02—05 | 任一必须阶段未形成结果时不得跳到后续完成态；允许按本表规定降级的阶段必须留下原因。图片可先成为候选，但品牌审查及图文一致性完成前不得自动入稿。 | 编辑器内的后续换图、局部重搜不是重新执行整条主流程。 | 新建项目能看到真实阶段；文案、图片、审查和2000px检查未全部结束时不能进入编辑器。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/server/modular-copy-generator.mjs`、`app/server/image-copy-consistency.mjs`、`app/tests/workflow-gates.test.mjs`、`app/tests/copy-generation.test.mjs`；本批真实证据见 `audit/evidence/2026-08-31-DeepSeek文案流式并发整改/`。 |
| FLOW-002 | 新智能体只有文案、事实核验、图片蓝图、预算内图片搜索、候选终审、自动放置、最终文案—图片一致性复核和实际 2000px 版面检查全部达到允许终态，才到100%并进入编辑器，不设置98%智能体状态。普通品牌表达建议经用户明确接受后可用 `user_accepted_suggestion` 作为允许终态，但不得标成 `passed`；生成完成与商业图片授权分开，授权待确认不改变100%和可编辑状态。固定流程原有98%修订行为只属于其冻结基线。 | A+C | 任务阶段状态、逐规则报告、用户决定、图片位终态、版面检查和授权状态 | 普通建议先提供局部修正；用户接受时保留问题/规则ID、用户决定和时间。事实、费用、履约、安全、内部信息、结构或必需图片硬问题必须修复或由专门门禁处理，不能使用接受建议终态；系统异常进入“生成失败”。 | `manual_review` 候选可保留，但必需位没有真实可用图片时不能100%；商业授权待确认可进入编辑器，正式导出仍被独立门禁阻止。 | 智能体仅在全部生成门禁允许且画布无空白、不合格或AI生成图时显示100%；正式导出还必须具备授权确认。 | `产品决定已写回，待独立复核`：规则口径已确定，智能体程序、状态和导出门禁尚未实现。 |
| FLOW-003 | 不设置面向用户的独立图片人工审核页。后台保留 `auto_approved/manual_review/hard_rejected` 三态；用户在编辑器换图体验中处理候选。 | A | 04 图片规则、当前产品决定 | 若旧组件仍存在但未路由，可保留为遗留代码；不得重新接入流程。 | 独立审查窗口和编辑器换图弹窗不属于“生成前独立审核页”。 | 当前路由直接从完整生成进入编辑器，未渲染独立审核步骤。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/tests/workflow-gates.test.mjs`；真实阶段快照见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/03-generation-result.json`。 |
| FLOW-004 | 新智能体最低视觉覆盖固定为：封面至少1张真实可用图片、每个实际展示的酒店模块至少1张主图、每个实际展示的 DAY 至少1张主图。第二张、装饰图和细节图属于可选位；无合格图时移除并自动重排。最终仍显示的每个图片位都必须真实、可解码并符合采用资格。必要性由版本化产品配置和程序判定，AI只能建议可选增量，不能新增、取消或降级必需位。 | A+C+D | 产品配置、真实展示模块、搜索预算、终审、用户锁定结果与实际版面 | 必需位无合格图时进入“待用户确认图片”，不得100%或进入编辑器；提供目标位重搜、用户选择可采用候选及上传已授权素材。禁止用空白框、占位图、演示图、无关图、不合格图、硬拒绝图或AI生成图补位。 | 被移除的可选位不属于客户成品空白位；固定流程冻结行为不作为智能体口径。 | 2000px客户预览中封面、每个展示酒店和每个展示DAY均满足最低覆盖，且所有保留图片位真实可用。 | `产品决定已写回，待独立复核`：最低表和职责已确定，程序配置、动态重排、等待状态与专项测试尚未实现。 |
| FLOW-005 | `manual_review` 表示候选未达到自动采用标准：可能是无硬错误但软偏好/把握不足，也可能是审核服务未完成判断；硬拒绝候选不可采用。manual 候选本身不要求生成前清零，但若它对应缺少真实可用图片的必需视觉位，则必须等待用户采用合格候选、上传素材或完成受控重搜；非必需位可移除重排。 | A+D | 04 三态、图片位必要性与审核错误策略 | manual 候选进入编辑器候选库，由用户查看来源和图片后决定；审核超时、429、5xx、网络或非法返回不得推断为硬拒绝，也不得自动采用；已确认硬拒绝无采用入口。 | 用户上传的自有素材按上传规则处理，不继承自动候选状态。 | 非必需 manual 候选不阻断；必需视觉缺图准确进入待用户确认且不得正式输出；审核系统错误可追溯，硬拒绝始终不可选。 | `部分落实，待程序整改`：三态与错误降级已存在；尚需将必需视觉缺图接入等待状态和导出门禁。 |
| FLOW-006 | 编辑器统一承接换图、跨模块移动、本地上传、删除和单位置重搜；具体采用资格、移动语义、决定记录与锁定规则唯一服从 `IMG-014/015`。 | A | 稳定 `slotId`、用户操作、项目数据 | 图片操作失败时不得破坏原项目状态；具体回滚与锁定要求见 `IMG-014/015`。 | 自动采用的未锁图片仍可被受控版面复查处理。 | 编辑器具备统一入口，图片操作行为通过 `IMG-014/015` 验收。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/tests/workflow-gates.test.mjs`；真实阶段快照见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/03-generation-result.json`。 |
| FLOW-007 | 新智能体进度必须来自真实阶段、动作、数量和耗时并单调递增；全部生成门禁达到允许终态才显示100%。页面区分“生成完成”“待事实确认”“待用户确认图片”和“生成失败”；普通表达建议被用户接受后记录 `user_accepted_suggestion`，它允许完成但不等于规则通过。商业授权状态独立显示，待确认不降低100%但阻止正式导出。 | A | 服务端任务状态、逐规则结果、用户决定、授权状态与统计 | 长阶段显示真实动作和耗时；硬问题继续锁定；系统失败显示真实原因。同一位置的多条底层检查记录合并展示用户问题数，同时保留原始审计记录。 | 固定流程的98%修订状态不进入智能体状态机；`user_accepted_suggestion` 只适用于普通品牌表达建议。 | 进度、问题状态、接受决定、授权门禁、编辑器入口与服务端状态一致，不把接受建议或授权待确认伪装成规则通过。 | `产品决定已写回，待独立复核`：智能体状态与授权分离口径已确定，程序尚未实现。 |
| FLOW-008 | 人工验收发生在编辑后复核或独立审查，不得被解释为生成中的新增门禁。执行窗口只能提交“待独立复核”，无权自行宣布验收通过或关闭台账。 | D | 审查台账、执行证据、正式成品 | 证据不足保持待复核；独立审查不通过时按具体证据退回修复。 | 产品讨论确认需求不是独立技术验收。 | 台账状态只由审查窗口在真实新项目复测后关闭。 | `自动支持已落实，结论无法自动化`：`audit/PROJECT_HANDOFF.md`、`audit/审查问题台账.md` 与本批真实证据；独立结论必须由审查窗口给出。 |
| FLOW-009 | 当前产品定位为本机单人使用 MVP，不承诺多人协同、云端持久化或 SaaS 级权限与审计。 | A+D | 当前项目范围 | 超出范围的需求进入产品决策，不在现有流程中暗示已支持。 | 本地 Sites 交付兼容不等于线上多租户产品。 | 对外说明与真实能力一致。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/tests/workflow-gates.test.mjs`；真实阶段快照见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/03-generation-result.json`。 |
| FLOW-010 | 任何重要修改均须形成执行交接：任务/问题编号、修改文件、自动化结果、全新项目真实回归、证据路径、剩余风险和待独立复核项。 | D | 正式任务书、测试、运行证据 | 缺少真实回归时必须明确写明，不得用旧演示或历史截图替代。 | 小型只读核查可按风险缩减证据。 | 交接可让独立审查复现结论。 | `自动支持已落实，结论无法自动化`：`audit/PROJECT_HANDOFF.md`、`audit/审查问题台账.md` 与本批真实证据；独立结论必须由审查窗口给出。 |

## 异常状态分类

审核发现业务语义问题时，用户流程统一为“审核只报告 → 同批问题由总智能体判断 → 程序校验并执行目标动作 → 只复查改变目标”。页面应区分审核中、智能体判断中、目标处理中和等待用户确认；审核结果本身不得直接触发整模块重生成、整批重搜或完成状态。

- 事实或费用冲突：按 02 阻止生成或返回用户确认。
- 普通品牌表达建议：按 03 以一个 DAY、酒店或小模块为单位有限局部重写；用户明确接受剩余建议后记为 `user_accepted_suggestion`，保存问题/规则ID、决定与时间，可作为智能体完成的允许终态，但不标 `passed`。
- 事实、费用、履约、安全、内部信息、结构和必需图片硬错误：进入对应阻塞/待确认状态，不得用文案改写或 `user_accepted_suggestion` 绕过；未解决前不得100%进入编辑器。
- 系统或外部服务异常导致任务无法形成可解释结果：进入“生成失败”，保留任务证据和可重试入口。
- 图片无合格结果：按04区分必需与非必需视觉位；非必需位从客户版面移除并重排，必需位进入待用户确认，可单位置重搜、选择可采用候选或上传已授权素材，不得用空白或不合格图片完成。
- 商业图片授权：与生成100%分开；授权待确认时可进入编辑器且生成状态保持完成，正式导出必须由用户确认权利或采用管理员预先批准素材。拒绝授权时替换图片、补充授权或停止导出，不回退重生、重审或98%。
