# 01 产品流程与完成状态

版本：1.9
生效日期：2026-09-07
适用范围：从资料上传到编辑、导出、执行交接与独立审查的产品流程和完成状态。数据、文案、图片、视觉和运行细则分别服从 02—06。

## 执行类型

- `A 程序强制`：确定性流程或状态门禁。
- `B AI生成`：模型承担的文案或视觉策划阶段。
- `C 生成后检查`：生成、放置或渲染后的质量检查。
- `D 人工验收`：业务、授权、审美或独立复核。

## Simple Skill Pipeline 适用边界

`simple-skill-pipeline` 使用 Planner → Copy/Image Skills → Program/Renderer 最小职责链，不启用 `review_decision`、finding package、多层 Reviewer、Reviewer 发现问题后自动重生成、图片自动第二轮搜索、自动定向重搜或 stage budget 驱动业务流程。下文仍出现这些名称的旧执行说明只保留为 `shadow-planner` 历史范围，不适用于新执行器；内容事实、完成状态、图片必要性、客户隔离和导出安全规则继续有效。

新执行器默认按最小责任单元处理失败：单个 DAY、酒店图片位或可选模块失败只记录该单元并允许无依赖任务继续；未解决的必需问题可以阻止最终 100%，但不能阻止其他可执行任务先完成。只有主输入无法解析、核心事实结构无法建立、关键事实/费用发生无法继续的冲突、项目保存系统失败，或其他确实使所有后续任务无法继续的项目级错误，才中断整条链。

编辑器同时承担“完成后调整”和“局部问题收尾”两种用途。输入与核心事实结构有效时，局部文案失败、必需图片未找到或单项审核服务异常必须先形成 2000px 可编辑草稿，并在编辑器集中提醒对应酒店、DAY或图片位；这些问题仍阻止100%和正式导出。草稿可编辑不等于生成完成，也不得用空白、不合格图片或缺失必需内容伪装正式成品。

## 最终规则

| 规则ID | 最终规则 | 执行类型 | 输入或依据 | 失败/异常处理 | 不适用边界 | 验收要求 | 当前落实状态与证据入口 |
|---|---|---|---|---|---|---|---|
| FLOW-001 | 标准主流程固定为：单份 Excel 上传 → 确定性解析 → 人工事实确认 → 生成整程内容与视觉主线 → 客户文案模块与图片蓝图/候选搜索并行 → 自动检查与目标任务处理 → 生成实际 2000px 版面。没有未解决必需问题时形成正式版本并到100%；存在局部文案、图片或审核问题时形成可编辑草稿并进入编辑器收尾。并行只改变等待顺序，不省略任何已执行阶段。 | A+B+C | 用户确认事实、02—05 | 项目级致命问题阻止后续；局部问题不得跳成完成态，但也不得阻断草稿渲染，必须留下原因和对应编辑入口。未完成终审的图片不得自动入稿。 | 编辑器内的后续换图、局部重搜不是重新执行整条主流程。 | 新建项目能看到真实阶段；局部失败可进入带提醒的草稿编辑器，只有文案、图片和2000px检查全部满足正式条件才显示100%并开放正式导出。 | `已落实，待独立复核`：`app/server/simple-pipeline-executor.mjs`、`app/server/simple-renderer.mjs`、`app/server/simple-manual-images.mjs`、`app/src/Workspace.jsx`、`app/src/AgentWorkspace.jsx` 与 `app/tests/simple-pipeline*.test.mjs`。 |
| FLOW-002 | 新智能体不设置98%智能体状态。主输入和核心事实有效时，局部文案、图片或审核问题以 `partial` 生成可编辑草稿；只有文案、事实核验、图片采用和实际 2000px 版面全部达到允许终态才显示100%。普通品牌表达建议经用户明确接受后可用 `user_accepted_suggestion` 作为允许终态，但不得标成 `passed`。可编辑草稿、100%生成完成与商业图片授权分别判断。 | A+C | 任务阶段状态、逐规则报告、用户决定、图片位终态、版面检查和授权状态 | 局部问题在编辑器集中提醒并保留目标修复入口；事实、费用、履约、安全、内部信息、结构或必需图片硬问题不能伪装为完成。项目级致命问题进入“生成失败”。 | `manual_review` 候选可保留；必需位没有真实可用图片时允许编辑草稿，但不能100%或正式导出。 | 页面明确区分“可编辑草稿”和“生成完成”；仅在全部生成门禁允许且正式画布无空白、不合格或AI生成图时显示100%，正式导出还必须具备授权确认。 | `已落实，待独立复核`：`app/server/simple-pipeline-executor.mjs`、`app/server/simple-renderer.mjs`、`app/server/simple-manual-images.mjs`、`app/src/AgentWorkspace.jsx` 与专项测试。 |
| FLOW-003 | 不设置面向用户的独立图片人工审核页。后台保留 `auto_approved/manual_review/hard_rejected` 三态；用户在编辑器换图体验中处理候选。 | A | 04 图片规则、当前产品决定 | 若旧组件仍存在但未路由，可保留为遗留代码；不得重新接入流程。 | 独立审查窗口和编辑器换图弹窗不属于“生成前独立审核页”。 | 当前路由直接从完整生成进入编辑器，未渲染独立审核步骤。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/tests/workflow-gates.test.mjs`；真实阶段快照见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/03-generation-result.json`。 |
| FLOW-004 | 新智能体最低视觉覆盖固定为：封面至少1张真实可用图片、每个实际展示的酒店模块至少1张主图、每个实际展示的 DAY 至少1张主图。第二张、装饰图和细节图属于可选位；无合格图时移除并自动重排。正式版本仍显示的每个图片位都必须真实、可解码并符合采用资格。必要性由版本化产品配置和程序判定，AI只能建议可选增量，不能新增、取消或降级必需位。 | A+C+D | 产品配置、真实展示模块、搜索预算、终审、用户锁定结果与实际版面 | 必需位无合格图时生成可编辑草稿并在编辑器提醒对应位置，提供目标位重搜、用户选择可采用候选及上传已授权素材；不得100%或正式导出。禁止用占位图、演示图、无关图、不合格图、硬拒绝图或AI生成图补位。 | 草稿编辑器中的待补图提示不属于正式客户成品；被移除的可选位不属于客户成品空白位。 | 草稿能进入编辑器并准确提示缺图；正式2000px版本中封面、每个展示酒店和每个展示DAY均满足最低覆盖。 | `已落实，待独立复核`：`app/server/simple-manual-images.mjs`、`app/server/simple-renderer.mjs`、`app/src/AgentWorkspace.jsx` 与 `app/tests/simple-manual-images.test.mjs`。 |
| FLOW-005 | `manual_review` 表示候选未达到自动采用标准：可能是无硬错误但软偏好/把握不足，也可能是审核服务未完成判断；硬拒绝候选不可采用。manual 候选本身不要求生成前清零；若它对应缺少真实可用图片的必需视觉位，则进入可编辑草稿，由用户采用合格候选、上传素材或主动重搜；非必需位可移除重排。 | A+D | 04 三态、图片位必要性与审核错误策略 | manual 候选进入编辑器候选库，由用户查看来源和图片后决定；审核超时、429、5xx、网络或非法返回不得推断为硬拒绝，也不得自动采用；已确认硬拒绝无采用入口。 | 用户上传的自有素材按上传规则处理，不继承自动候选状态。 | 非必需 manual 候选不阻断；必需视觉缺图准确进入草稿提醒且不得100%或正式输出；审核系统错误可追溯，硬拒绝始终不可选。 | `已落实，待独立复核`：三态与错误降级、审核超时、草稿提醒和导出门禁已接入 Simple Pipeline，仍需真实新项目复核。 |
| FLOW-006 | 编辑器统一承接换图、跨模块移动、本地上传、删除和单位置重搜；具体采用资格、移动语义、决定记录与锁定规则唯一服从 `IMG-014/015`。 | A | 稳定 `slotId`、用户操作、项目数据 | 图片操作失败时不得破坏原项目状态；具体回滚与锁定要求见 `IMG-014/015`。 | 自动采用的未锁图片仍可被受控版面复查处理。 | 编辑器具备统一入口，图片操作行为通过 `IMG-014/015` 验收。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/tests/workflow-gates.test.mjs`；真实阶段快照见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/03-generation-result.json`。 |
| FLOW-007 | 新智能体进度必须来自真实阶段、动作、数量和耗时并单调递增；全部生成门禁达到允许终态才显示100%。生成页默认采用定制师工作视角：上半部分回答“正在制作什么、确认事实是否保留、本次定制重点是什么”，下半部分用业务语言展示客户行程制作进度和当前动作，并说明完成后仍可调整。Skill 名称、责任单元、计划/运行 ID、能力版本和内部目标路径只能放入默认收起的“管理员运行详情”。页面区分“生成完成”“可编辑草稿”“待事实确认”和“生成失败”；草稿按酒店文案、交通文案、每日文案和待补图片等用户可理解类别集中提醒。商业授权状态独立显示，待确认不降低100%但阻止正式导出。 | A | 服务端任务状态、逐规则结果、用户决定、授权状态与统计 | 长阶段显示真实业务动作和耗时；单项能力结束不得把整个阶段误标完成；系统失败显示用户可理解原因。同一位置的多条底层检查记录合并展示用户问题数，同时在管理员详情保留原始审计记录。 | 固定流程的98%修订状态不进入智能体状态机；`user_accepted_suggestion` 只适用于普通品牌表达建议。 | 默认视图不出现 Copy/Image Skill、Simple Pipeline、责任单元、内部 ID 或目标路径；进度、问题状态、提醒、授权门禁、编辑器入口与服务端状态一致，不把草稿或授权待确认伪装成正式完成。 | `已落实，待独立复核`：`app/src/lib/agentProgressView.js`、`app/src/Workspace.jsx`、`app/server/agent-planner-app.mjs`、`app/server/simple-manual-images.mjs` 与专项测试。 |
| FLOW-008 | 人工验收发生在编辑后复核或独立审查，不得被解释为生成中的新增门禁。执行窗口只能提交“待独立复核”，无权自行宣布验收通过或关闭台账。 | D | 审查台账、执行证据、正式成品 | 证据不足保持待复核；独立审查不通过时按具体证据退回修复。 | 产品讨论确认需求不是独立技术验收。 | 台账状态只由审查窗口在真实新项目复测后关闭。 | `自动支持已落实，结论无法自动化`：`audit/PROJECT_HANDOFF.md`、`audit/审查问题台账.md` 与本批真实证据；独立结论必须由审查窗口给出。 |
| FLOW-009 | 当前产品定位为本机单人使用 MVP，不承诺多人协同、云端持久化或 SaaS 级权限与审计。 | A+D | 当前项目范围 | 超出范围的需求进入产品决策，不在现有流程中暗示已支持。 | 本地 Sites 交付兼容不等于线上多租户产品。 | 对外说明与真实能力一致。 | `已落实，待独立复核`：`app/server/workflow-state.mjs`、`app/server/app.mjs`、`app/tests/workflow-gates.test.mjs`；真实阶段快照见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/03-generation-result.json`。 |
| FLOW-010 | 任何重要修改均须形成执行交接：任务/问题编号、修改文件、自动化结果、全新项目真实回归、证据路径、剩余风险和待独立复核项。 | D | 正式任务书、测试、运行证据 | 缺少真实回归时必须明确写明，不得用旧演示或历史截图替代。 | 小型只读核查可按风险缩减证据。 | 交接可让独立审查复现结论。 | `自动支持已落实，结论无法自动化`：`audit/PROJECT_HANDOFF.md`、`audit/审查问题台账.md` 与本批真实证据；独立结论必须由审查窗口给出。 |

## 异常状态分类

审核发现业务语义问题时，用户流程统一为“审核只报告 → 同批问题由总智能体判断 → 程序校验并执行目标动作 → 只复查改变目标”。页面应区分审核中、智能体判断中、目标处理中和等待用户确认；审核结果本身不得直接触发整模块重生成、整批重搜或完成状态。

- 事实或费用冲突：按 02 阻止生成或返回用户确认。
- 普通品牌表达建议：按 03 以一个 DAY、酒店或小模块为单位有限局部重写；用户明确接受剩余建议后记为 `user_accepted_suggestion`，保存问题/规则ID、决定与时间，可作为智能体完成的允许终态，但不标 `passed`。
- 事实、费用、履约、安全、内部信息和核心结构的项目级致命错误：阻止草稿渲染并进入对应失败/确认状态。局部文案或必需图片问题：允许进入带提醒的可编辑草稿，但不得用文案改写或 `user_accepted_suggestion` 绕过，未解决前不得100%或正式导出。
- 系统或外部服务异常导致任务无法形成可解释结果：进入“生成失败”，保留任务证据和可重试入口。
- 图片无合格结果：按04区分必需与非必需视觉位；非必需位从客户版面移除并重排，必需位进入待用户确认，可单位置重搜、选择可采用候选或上传已授权素材，不得用空白或不合格图片完成。
- 商业图片授权：与生成100%分开；授权待确认时可进入编辑器且生成状态保持完成，正式导出必须由用户确认权利或采用管理员预先批准素材。拒绝授权时替换图片、补充授权或停止导出，不回退重生、重审或98%。
