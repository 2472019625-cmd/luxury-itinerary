# 04 图片搜索与使用规范

版本：1.1  
生效日期：2026-08-29  
适用范围：图片蓝图、图片位、搜索、下载、审核、采用、去重、编辑器换图、来源与授权、2000px 版面复查。本文是图片领域唯一当前正式规则。

## 已裁定流程口径

- 不设置独立图片人工审核页。
- 后台保留 `auto_approved/manual_review/hard_rejected`，但 `manual_review` 不阻止任务完成或进入编辑器。
- 只有成功完成终审、达到自动采用标准且已保存到本地的图片可自动入稿。
- 没有自动通过图的图片位在预算耗尽后明确留空，属于完成状态。
- 用户在编辑器选择、上传、移动或清空图片后必须锁定。

## 最终规则

| 规则ID | 最终规则 | 执行类型 | 输入或依据 | 失败/异常处理 | 不适用边界 | 验收要求 | 当前落实状态与证据入口 |
|---|---|---|---|---|---|---|---|
| IMG-001 | 先从确定性行程和真实版面建立稳定 `slotId`，再由整程视觉蓝图规划，禁止先搜图再靠标题、地点模糊匹配或请求顺序填充。 | A+B | `itineraryFacts/layoutSlots/lockedImages` | 蓝图最多修正一次；仍失败则受影响图片位留空完成，不回退旧模糊绑定。 | 单位置重搜只读取既有目标蓝图。 | 搜索、候选、采用、重搜和复查全程使用同一 `slotId`。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-002 | 视觉蓝图读取整份行程、每天完整内容、体验状态和全程视觉节奏，只规划输入中存在的图片位，不修改任何事实或发明 URL。 | B+A | 确定性事实、真实图片位、项目用途 | 非法 JSON、重复/未知 slot、无事实依据或覆盖锁定项时程序拒绝并修正一次。 | 模型可决定视觉角色和搜索表达，不能决定费用或履约。 | 蓝图记录版本、模型、提示词版本和输入/版面指纹。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-003 | 图片故事按体验结构与视觉角色建立：转场/返程日目标 1—2 张，普通核心体验日默认目标 2 张，事实丰富且角色不同可 3—4 张；最终合格图可少于目标。 | A+B+C | 经过校验的 `spots/imageRoles`、DAY 事实 | 不为凑数量创建重复或无依据位置；终审失败留空。 | 目标数量不是客户成品最低图数，也不是放宽质量门槛的理由。 | 同日图片承担 environment/action/detail/transition/closing 等不同角色。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-004 | 图片必须真实证明相邻文案；封面重目的地代表性和标题承载，酒店重身份/真实空间，餐饮重具体用餐形态，交通重类别，每日体验重活动主体。 | B+C+D | 相邻文案、模块职责、来源证据 | 主体、地点、品牌或模块错配时硬拒绝或重搜目标位。 | 同等级交通示意允许，但不能形成具体车型承诺。 | 抽查五类模块均图文一一对应。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-005 | 图片调性须真实、干净、高端、有品质和目的地辨识度；脏乱、廉价、卫生观感差、过度滤镜、低清、明显 AI 感或负面人物状态不得入稿。 | B+C+D | 品牌调性、视觉审核 | 命中硬质量问题则拒绝；主观高级感不足但无硬伤可作 manual 候选。 | 主观审美最终由人工验收，不新增生成门禁。 | 最终成品无明显廉价、杂乱或不适图。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-006 | `mustHave` 只放地点、品牌、活动、主体等不可妥协事实；`prefer` 只排序；`forbid` 处理错地点、错主体、水印、低质、脏乱等硬错误。 | A+B+C | 图片位事实与文案 | 不得因未满足日落、泳池、花园、香槟等软偏好直接硬拒绝。 | 客户文案明确承诺的设施可从偏好升级为硬要求。 | 软偏好不足候选进入 manual 而非静默消失。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-007 | 来源优先级为用户原始/已授权素材、酒店景区运营方官方、旅游局/官方媒体、可信联盟或媒体、授权清晰图库、其他第三方；官网身份按域名判断。 | A+B+D | 来源页面与域名 | 无法确认来源仍可作为候选但不得伪标官方；商业发布前人工复核授权。 | 来源可信不能替代视觉主体检查。 | 来源账本记录页面、原图、来源类型和官方标记。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-008 | 单槽每轮最多 5 个来源页、每页 12 图、下载 12、初审 4、终审 2；所有图片位最多 2 轮，不因高优先级增加第三轮。搜索下载并发固定 3；视觉审核使用独立队列，初始并发 2、稳定后最高 3。搜索下载、初审、终审和单槽总流程分别使用独立超时预算。 | A | 搜索管线集中配置与候选状态 | 出现可用 `manual_review` 候选即停止该槽自动重搜；达到两轮仍无候选则留空完成。审核超时、429、5xx、网络错误或非法返回降级为可人工决定候选，不得记为 `hard_rejected`，也不得因此启动下一轮。只可放宽 prefer，不能放宽硬事实或 forbid。 | 用户主动单槽重搜是新的受控请求。 | 测试与真实账本证明搜索峰值不超过3、审核峰值不超过配置、每槽不超过2轮、审核系统错误未进入硬拒绝；固定槽位结果不依赖网络完成顺序。 | `已落实，待独立复核`：`app/config/image-pipeline.mjs`、`app/server/image-pipeline.mjs`、`image-audit.mjs`、`app/tests/image-pipeline.test.mjs`；三次禁用缓存真实回归及中位数见 `audit/evidence/2026-08-31-分级推理与图片超时整改/00-benchmark-summary.json`。 |
| IMG-009 | 候选进入视觉模型前须可公开 HTTP/HTTPS 获取、可解码，格式 JPEG/PNG/WebP，至少 900×500，比例 0.65—3.2，单文件不超过 14MB；禁止访问本机、内网和私有 IP。 | A | 下载响应与图像元数据 | 技术失败记入账本，不伪造缩略图，不进入终审。 | 用户本地上传使用独立上传校验和存储路径。 | 失败记录含阶段、原因、地址和来源页。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-010 | 每个成功下载的候选有独立稳定记录 ID，不能直接用内容哈希充当记录身份；记录所属 slot、轮次、本地预览、原图、来源、尺寸、评分、阶段、状态和原因。 | A | 下载与审核过程 | 任一阶段不得静默丢弃；同一记录 ID 不得出现冲突状态。 | 内容哈希继续用于文件去重。 | 账本记录数等于唯一记录 ID 数，界面缩略图与本地文件一一对应。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-011 | 三态定义：`auto_approved` 为无硬错且达到模块自动采用标准；`manual_review` 为无硬错但软偏好/把握不足，或审核服务未能完成判断；`hard_rejected` 仅用于破图、水印、错地点/主体、严重低质或重复等已经得到证据的硬错。 | A+C | 终审结果、审核错误分类与单一运行时策略 | 审核超时、429、5xx、网络错误和非法 JSON 不得推断图片有硬错：候选保存为 `manual_review`、可在编辑器决定、不可自动采用。硬拒绝不可选。 | manual 不是“必须处理完才能完成”的状态，也不代表图片已通过终审。 | 五类模块均产生一致三态；专项测试和真实账本证明系统错误未进入硬拒绝。 | `已落实，待独立复核`：`app/server/image-audit.mjs`、`image-pipeline.mjs`、`app/tests/image-pipeline.test.mjs`；三次真实账本见 `audit/evidence/2026-08-31-分级推理与图片超时整改/formal-*/08-image-ledger.json`。 |
| IMG-012 | 自动采用必须同时满足：终审成功、硬条件通过、模块阈值达到、全局去重通过、文件已本地保存；远程 URL、Data URL 搜索结果或未终审候选不得自动入稿。 | A+C | 终审、去重、本地资源 | 任一条件不满足则不自动采用，保留候选或留空。 | 用户本地上传按用户锁定处理。 | 客户 JSON 最终图只含可用本地资源、用户上传资源或受支持 Blob。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-013 | 不设置独立审核页；manual 候选与缺图原因进入编辑器换图弹窗。弹窗主要入口为“适合当前位置、全部行程图片、本地上传”，不在客户画布展示内部评分和审核术语。 | A | 候选库、当前 slot | 候选加载失败显示来源与错误，不显示浏览器破图。 | 独立审查可查看内部账本。 | 可从空位或现有图打开换图，查看候选、来源摘要和当前位置。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-014 | 用户可采用 manual 候选，但硬拒绝没有采用入口；采用、上传、跨模块移动或清空均记录决定、时间、来源并锁定。选择已使用图默认“移动”，原位置留空。 | A+D | 用户操作与图片安全状态 | 操作失败回滚到原状态；不得静默复制或覆盖其他锁定决定。 | 用户拥有无水印授权版本时应上传替代文件，而不是采用硬拒绝候选。 | 刷新/重开后决定仍存在，自动重搜和复查不覆盖。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-015 | “为当前位置搜索更多”只重搜目标 `slotId`，使用目标蓝图，预载全图已用图片参与去重；只更新目标候选/来源/失败记录，规则指纹变化使旧审核缓存失效。 | A+C | 当前项目、目标 slot、已有采用图 | 不修改其他槽位或用户锁；失败后保留原状态和原因。 | 整程事实或模板图片位实质变化才重做整份蓝图。 | 单槽运行账本无重复合并，其他槽位哈希不变。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-016 | 全图去重覆盖封面、酒店、特色餐饮、交通和每日体验：SHA-256 防相同文件，感知哈希防缩放/轻裁切，同场景连拍和同来源图由视觉复查处理。 | A+C+D | 所有采用图和候选 | 重复图拒绝或移动，不允许复制；锁定图只提示用户，不自动替换。 | 不同图但同一酒店的不同主体可用，前提是视觉角色不同。 | 最终 PNG 无文件级和视觉近似重复。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-017 | 默认 `cover`；车辆外观/内饰、房间结构、餐饮细节等证据型图片可用 `contain`。复合卡最多两图，每图有 `label` 且证明不同子体验；2000px 不可辨认时拆卡或保留强项。 | A+C+D | 图片类型、卡片比例、最终渲染 | 严重裁切仅重搜/换图目标位或调整 fit；不缩到不可识别。 | 封面氛围图优先 cover。 | 最终成品逐图可辨认，车辆不裁掉关键主体。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-018 | 图片放入真实版面后必须进行 2000px 全局复查：错模块、图文不符、错地点/主体、重复、裁切、封面代表性、双图职责、顺序、色调、异常空白。只处理失败 slot。 | C+D | 实际客户渲染与图片位 | 图片问题单槽重搜/替换/留空；用户锁图只提示；不得重生成文案或整份蓝图。 | 全文文案与费用检查见 03、05。 | 复查结果有记录，失败位受控重试且不影响其他位。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |
| IMG-019 | 来源账本用于追溯，不等于商业授权；正式商业发布前人工确认许可、必要署名和用户上传素材使用权。 | A+D | 来源记录、授权条款 | 授权不明不得宣称已获授权；按业务决定替换、补授权或停止发布。 | 内部测试可使用测试素材，但不能当正式业务图片发布。 | 每张最终图有来源或用户上传记录，商业授权单独验收。 | `自动支持已落实，结论无法自动化`：`07-image-ledger.json` 保存来源与许可声明，正式版本页保存人工授权复核决定；商业授权结论必须人工判断。 |
| IMG-020 | 客户预览和成品不得显示候选状态、审核分数、失败原因、来源、授权提醒、“未通过终审”等内部图片用语；这些只在编辑器候选详情和内部账本出现。 | A+C | 客户字段白名单与可见文案 | 检出即清理并阻止正式输出。 | 客户可见的“待确认/自费可选”是业务状态，不是图片审核状态。 | 预览与 PNG 无内部审核文案。 | `已落实，待独立复核`：`app/server/image-*.mjs`、`page-images.mjs`、`app/src/lib/imageSlots.js`、`imageDecisions.js` 与图片测试；真实账本/编辑证据见 `audit/evidence/2026-08-29-程序规则缺口补齐/real-rule-2026-08-29T11-24-01-028Z/07-image-ledger.json`、`10-editor-and-single-slot-regression.json`。 |

## 图片位完成状态

图片位在以下任一条件成立时视为完成：自动采用本地图；已有用户锁定结果；预算内搜索/终审结束后留空；蓝图或搜索失败并记录原因后留空。`manual_review` 候选可同时保存在候选库，不改变图片位的完成状态。
