# 奢游行程工具工作约束

## 智能体试验工作区边界

- 本工作区路径为 `D:\奢游\行程单报价单\奢游行程单美化-智能体试验区`，专用分支为 `agent/shadow-planner-v1`。
- 本工作区只用于新智能体流程、影子任务规划器及其后续受控验证；不得在原固定流程目录中实施智能体代码。
- 原固定流程目录 `D:\奢游\行程单报价单\奢游行程单美化-本地版` 已由提交 `af61348` 和标签 `pre-agent-recovery-2026-09-01` 保护。除明确的旧流程修复任务外，不得在该目录继续智能体开发。
- 当前固定流程继续保留“98%可进入修订编辑器、预算内搜图结束后允许缺图留空”的既有行为；新智能体流程服从 `docs/产品设计/行程成品生成智能体产品蓝图.md`，执行“全部阶段完成后100%进入编辑器、最终实际显示图片位不得留空”的新口径。
- 同一项目不得在固定流程与智能体流程之间中途切换或共享完成状态。智能体试验必须使用独立入口、端口、浏览器数据和测试项目目录，不得占用原固定流程的4173端口。
- 当前第一阶段只允许建立规则与能力调度规范和影子任务规划器；影子规划只输出计划，不调用真实文案生成、联网事实搜索、图片搜索、自动放置或正式导出，除非后续任务书明确授权进入下一阶段。
- 智能体开发前完整读取本文件、产品蓝图和任务涉及的规则；旧固定流程的冻结基线与新智能体规则配置必须分别标注版本，单次任务不得同时读取两套运行口径。

## 唯一项目与冻结边界

- 新智能体流程的唯一开发源是本工作区 `D:\奢游\行程单报价单\奢游行程单美化-智能体试验区`；原本地版目录只运行和保护固定流程，不承接智能体修改。
- `D:\奢游\行程单报价单\奢游行程模板系统` 是冻结历史档案，只能查阅，禁止写入代码、规则、测试、证据或新成品。
- 当前项目使用 Git 与 GitHub 管理；`main` 只保存已确认的稳定版本，正式任务使用独立分支并保留可追溯提交。不得覆盖、回滚或清理用户已有成果。

## 规则入口

任何产品、数据、文案、图片、视觉、运行或验收改动开始前，完整读取 `rules/00-当前规则索引.md`，再读取任务所属的 01—06 正式规则和智能体产品蓝图。01—06 是规则内容来源；影子规划器开发前必须先完成智能体适用范围和版本配置，旧规则、任务书、交接和台账只用于历史追溯、状态和证据。

程序当前行为不能自动成为产品规则。Markdown 也不会自动被工具执行；真实落实必须对应确定性代码、专用 AI 提示词、生成后检查或人工验收，并由 `rules/规则迁移与程序落实对照表.md` 记录证据与缺口。

## 执行窗口职责

- 只实施产品讨论窗口已经明确确认的任务，严格遵守交接稿的范围、数据、交互、异常和验收标准。
- 发现冲突、遗漏或高风险时先指出；没有现成用户结论支持的重大冲突进入待产品决策，不擅自改变产品方向。
- 完成后记录问题编号、修改文件、自动化结果、全新项目真实回归、证据、剩余风险和待独立复核项。
- 执行窗口不得自行宣布独立验收通过或关闭台账。

## 长期不可违背决定

- “98%进入修订编辑器、预算结束后缺图可留空”只属于冻结的当前固定流程，不得作为新智能体的完成规则。
- 新智能体只有文案、事实核验、图片、审核和实际 2000px 检查全部结束并通过新规则门禁后才到100%并进入编辑器；最终实际显示图片位不得留空，非必需位可移除重排，必需位缺图进入用户确认。
- 不恢复独立图片审核页；`manual_review` 候选是否影响智能体完成，必须结合图片位必要性处理，不能直接继承固定流程的留空口径。
- 原始资料和用户确认事实高于 AI；AI 不得修改日期、人数、路线、酒店、晚数、餐食、交通、费用和履约状态。
- 内部成本、利润、底价、公式、内部备注和审核/来源信息不得进入客户成品。
- 自费、可选、待确认体验可成为卖点和主图，但必须同步真实状态与费用边界。
- 唯一视觉基准是 `assets/references/current-visual-baseline-kenya-8d-2000.png`；旧视觉母版已废止。
- 标准客户交付是一张宽 2000px 的 PNG。

## 开发与运行约束

- UI 在 `app/src/`，程序、服务、渲染器、依赖和构建均位于 `app/`；所有 npm 命令从 `app/` 执行。
- 保持 `app/.openai/hosting.json`、`app/worker/index.js`、`app/scripts/prepare-sites-build.mjs`、`app/tests/sites-worker.test.mjs` 的 Sites 交付能力。Sites 交接前运行 `npm run build` 和 `npm run test:sites`。
- 能在本环境启动服务和浏览器真实预览时，由执行窗口完成，不把可自行完成的启动步骤交给用户。
- 不修改或输出模型、API Key、接口地址、计费配置或任何 `.env` 文件，除非用户再次明确授权。
- 新验证必须使用新项目和新浏览器数据，不使用旧 `localStorage`、演示数据、历史截图或冻结旧项目成品证明当前实现。
